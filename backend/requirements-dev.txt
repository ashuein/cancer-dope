# Dev and test dependencies — install with:
#   pip install -r requirements.txt -r requirements-dev.txt

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0

# Linting
ruff==0.7.1

# Type checking
mypy==1.12.1

# Test utilities (httpx already in requirements.txt)
